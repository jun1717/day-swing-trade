# TRADING_RULES.md — v1 売買ルール（正式版）

このファイルが **v1 の売買ルールの唯一の正本** である。

- 発端の要件定義は `CODEX_HANDOFF.md`（v0.1）として参照する。**本ファイルと内容が異なる場合は、本ファイルを優先する。**
- 過去データでの検証結果は `RESEARCH_SUMMARY.md`。**そこで研究した条件の大半は正式ルールに採用していない。**
- 実装の設計は `DESIGN.md`、日々の操作は `README.md`。

2026-08 に研究フェーズを終了し、**フォワード運用フェーズ**へ移行した。

以後は「過去 32 件をもっとうまく捌く条件」を探さない。実際に使い、新しい事例を貯めることを優先する。

---

## 0. 3 つのラベル

ルール・設定・研究結果を混同しないため、すべてに次のどれかを付ける。

| ラベル | 意味 | 変更してよいか |
|---|---|---|
| **CONFIRMED** | v1 の正式ルール。売買判断の根拠にしてよい | 変えるならこの文書を改訂する |
| **PROVISIONAL** | 暫定値。最適値として証明されていない安全ガード | 実際の日足を見たフィードバックで調整してよい |
| **RESEARCH_ONLY** | 研究したが本番ルールには採用していない | 本番コードに入れてはいけない |

**最重要**: RESEARCH_ONLY のものを「だいたい正しそうだから」と本番に持ち込まない。

研究で分かったのは主に「**どれも決め手にならなかった**」ということであって、どれかが正解だったということではない。

---

## 1. 投資スタイル — CONFIRMED

- 対象: 日本株（監視リストの 98 銘柄）
- 時間軸: **日足ベースの短期スイング**、保有 数日〜2週間程度
- デイトレードではない。場中の常時監視を前提にしない
- 原則 **引け後に日足を確認 → 翌営業日の行動を決める**

> **運用設定（売買ルールではない）**: この前提を守るため、日次自動生成の ChatGPT bundle は当日セッション終了後のデータだけを正式（`session_status=FINAL`）として扱う。判定は `config.yaml: market_session`（Asia/Tokyo・大引け 15:30・データ確定待ち 16:00）と `src/swing_screener/market_session.py`。
>
> **これは売買パラメータではなく、データ確定待ちの運用設定である。**

### 最上位の原則 — CONFIRMED

> **買う前にシナリオを持つ。買った理由がなくなったら売る。想定と違ったら売る。**

- 損益額だけを理由に保有判断を変えない
- 「そのうち戻る」で保有を継続しない

### 狙う形 — CONFIRMED

**上昇トレンド中に形成された短期レンジの下限付近から、反発を確認して次の上昇波を取る。**

レンジ上限ブレイクを新規エントリーとして探すことは **しない**。

すでに保有している銘柄が上限を突破するのは歓迎で、保有継続の材料になる。

---

## 2. ユニバース — CONFIRMED

- 事前に選定した監視銘柄のみ（`data/watchlist.csv`）。全上場銘柄はスクリーニングしない
- `is_leader` / `watch_priority` は**売買条件ではない**。表示順の最後の補正のみ
- 株価フィルター: **2,000円以上 7,000円以下**（`config.yaml: price_filter`）
  - 銘柄マスターからは削除しない。その日の売買候補から外すだけ

---

## 3. ENTRY

### 3.1 CONFIRMED — 変更しない

| 条件 | 定義 | 場所 |
|---|---|---|
| 上昇トレンド | `close > MA25` | `rules/trend.py` |
| 移動平均 | 25日単純移動平均 | `config.yaml: ma.period` |
| 短期レンジ | 3〜10営業日の window から選ぶ | `config.yaml: range.min_days / max_days` |
| 下限反応 | レンジ品質の**評価軸**。2回で満点（→ §3.3）。**成立の必須条件ではない** | `config.yaml: range.min_lower_touches` |
| レンジ境界 | 一点ではなく **zone**（価格帯）として扱う | `rules/range_detect.py` |
| 下限付近 | 現在値がレンジ下限に近いこと | `rules/status.py` |
| **反発確認** | **終値 > 前日高値** | `rules/rebound.py` |
| 出来高 | **補助評価のみ。単独で ENTRY 判定に使わない** | `indicators/volume.py` |

ENTRY_CANDIDATE は上の全部を満たした状態を指す。

ただし「下限反応」は**回数のしきい値ではなく品質スコアの一項目**である（§3.3）。

> **ENTRY_CANDIDATE = 自動的に買う、ではない。** 必ず日足チャートを人間が見る。

### 3.2 PROVISIONAL — 暫定値

| パラメータ | 現在値 | 位置づけ |
|---|---|---|
| **`near.max_position_in_range`** | **0.65** | **最適値として証明されたものではない。** レンジ上側での遅い ENTRY を除外するための安全ガード |
| `near.lower_threshold_pct` | 2.0 | 「下限付近」の距離 |
| `near.lookback_days` | 3 | 直近何日の下限接触まで NEAR とみなすか |
| `near.break_tolerance_pct` | 1.5 | レンジ崩壊とみなす下抜け幅 |
| `ma_slope.*` | vs_n_days_ago / 5日 / 0.0% | MA25 上向きの判定方法 |
| `swing.*` | fractal / pivot_window=2 | swing high/low の検出方法 |
| `range_zone.*` | 上下 0.7% | レンジ境界 zone の幅 |
| `range_quality.*` | 重み・除外条件一式 | レンジらしさの評価 |
| `volume.*` | 0.80 / 1.30 / 1.20 | 出来高の状態区分 |

すべて `experimental.yaml` にある。

#### `0.65` について（重要）

過去 129 営業日・98 銘柄での検証結果:

| ガード | ENTRY件数 | レンジ内位置の中央値 | 形状 |
|---|---:|---:|---|
| 制限なし | 264 | 0.84 | 上限zone 113件・上限付近 45件を含む |
| 0.80 | 106 | 0.72 | 遅い ENTRY 74件 |
| 0.70 | 45 | 0.61 | 遅い ENTRY 13件 |
| **0.65（現行）** | **32** | **0.56** | **全件が「理想形（下限反発）」** |
| 0.60 | 18 | 0.49 | |
| 0.50 | 10 | 0.46 | |

0.65 を超えると、**レンジ上側からの遅い ENTRY が混ざり始める**。

それは `CODEX_HANDOFF.md` §21 が「新規エントリーに使わない」と決めた上限ブレイクそのものになる。

一方 0.65 が最良の成績を出したという証拠は無い（そもそも成績で選んでいない）。

**したがって 0.65 は「上側を拾わないための線」として暫定維持する。**

再最適化は行わない。

### 3.3 下限反応回数について（重要・2026-08 に表現を訂正）

**`min_lower_touches = 2` は「満点になる目標値」であって、成立の必須条件ではない。**

```text
下限反応は 2回以上を高く評価する（品質スコアで満点になる）。

1回でも、値幅の狭さ・値幅の収縮・出来高の減少・日数を含む
総合品質が range_quality.min_quality を超えればレンジは成立し、
下限付近＋反発確認が揃えば ENTRY_CANDIDATE になり得る。
```

計算は `rules/range_detect.py` の `_quality()`。

`min(touch_count / min_lower_touches, 1.0)` を重み `0.30`
（`experimental.yaml: range_quality.weights.lower_touches`）で足すだけで、**回数による除外は行わない**。

表示上は「目標 2回」に届かない場合 ✗ が付くが、これは `required=False` の参考表示である。

#### なぜ 2回以上が必須だと誤読されたのか

この文書の §3.1 が「下限反応 = レンジ下限付近で 2回以上」とだけ書き、必須条件の表に並べていたため。

**文書側の誤記だった。**

設計時点の資料は一貫して「目標値」として扱っている。

| 資料 | 記述 |
|---|---|
| `CODEX_HANDOFF.md` §15 | 「最低2回程度反応 **したものを高評価する**」「**目標**: `lower_touch_count >= 2`」「ただし zone 判定の誤差を考慮する」 |
| `CODEX_HANDOFF.md` §12 | 除外条件を明示列挙しているが、**下限反応回数は入っていない** |
| `DESIGN.md` range_detect | 除外条件と品質スコアを別の一覧にし、下限反応は**品質スコア側** |
| `experimental.yaml` | `range_quality.weights.lower_touches: 0.30`（= **重み**） |
| 実装 | **実装以来ずっと品質スコアの分母。除外条件に使ったことはない** |

決定的な根拠は §3.2 の 0.65 の検証表そのものである。

あの **264件 / 32件という数字は、下限反応 1回を許容する実装で得られたもの**で、`research/events_pos065.csv` の 32 件のうち **23 件が `lower_touch_count = 1`** である。

仮に 2回以上を必須にすると 32 件は 9 件になり、「0.65 なら全件が理想形（下限反発）」という §3.2 の根拠が別の話になる。

同じ理由で、現在の 98 銘柄でも影響は大きい（2026-08-10 時点の実測）:

```text
2回以上を必須にした場合
候補 13件 → 2件
（NEAR 3→2 / RANGE 10→0 / OUT 85→96）
```

**したがってロジックは変更しない。**

期待値は `tests/test_lower_reaction_rule.py` で固定した。

将来この仕様を変えるなら、先にこの文書を改訂し、§3.2 の根拠データを取り直すこと。

---

## 4. 初期STOP — CONFIRMED

```text
initial_stop = range_lower × 0.995
```

レンジ下限の 0.5% 下（`config.yaml: stop.buffer_pct`）。

**変更しない。**

### 4.1 STOP は最大損失を保証する価格ではない — 重要

過去検証で、初期STOP に到達した 31 件のうち **10 件（32%）が寄付でギャップ割れ**していた。

割れ幅の中央値は **-1.94%**。

```text
想定損失 = entry_price − initial_stop
↑ これは成立しない前提

実際 = entry_price − 実際の約定価格
↑ ギャップ次第でもっと大きい
```

- 日足の終値ベースで判定する運用である以上、STOP 価格での約定は仮定できない
- 将来ポジションサイズを決めるときは、このギャップリスクを織り込む必要がある
- **ただし v1 ではポジションサイズ計算ルールを作らない**（§5）

### 4.2 STOP の引き上げ — v1 では自動化しない

新しい押し安値の形成後に STOP を引き上げる考え方（`CODEX_HANDOFF.md` §30）は**維持する**。

ただし機械判定はしない。

理由は §6 と `RESEARCH_SUMMARY.md`。

---

## 5. ポジションサイズ — CONFIRMED（自動化しない）

- 購入株数は**ユーザーが決める**
- ツールは計算も推奨もしない
- リスク許容度・資金量・ギャップリスク（§4.1）を人間が考える

---

## 6. EXIT — v1 では完全自動化しない（最重要）

### 6.1 なぜ自動化しないのか

警戒陰線・`warning_low`・`reference_high`・押し安値・trail stop を機械化しようと5 種類の研究を行った結果、次が分かった。

- **WARNING を早く出すと、正常な調整で早く降りる**
- **WARNING を遅くすると、WARNING が出ないまま初期STOPまで戻る**
- **`reference_high` を緩めても、利益の吐き出しは根本的に改善しない**
  - 最大含み益 +10% 以上のケースで残せた割合の中央値は 5 案とも 1.0〜1.4%
- **trail 成立件数を増やすほど、降りたあとにさらに上昇する副作用が増える**
  - trail で降りた件の 86〜100% が EXIT 後に +3% 以上上昇

さらに、判断材料になる ENTRY イベントが **32 件しかない**。

これ以上条件を詰めると過去 32 件への過剰適合になる。

**よって v1 では「売れ」と断定する機械判定を持たない。**

詳細は `RESEARCH_SUMMARY.md`。

### 6.2 EXIT で正式に残す考え方 — CONFIRMED

`CODEX_HANDOFF.md` §30 の思想はそのまま維持する。

ただし **どれも人間が判断する**。

1. **普通の陰線 1 本では売らない**
2. **レンジ上限到達だけでは売らない**
3. **上限突破後も上昇シナリオが続くなら HOLD**
4. **明確に上昇シナリオが崩れたら撤退する**
5. **警戒陰線（最初の陰線）の安値割れは、利確を検討する材料**
6. **新しい押し安値の形成後は、STOP 引き上げを検討する**
7. **大陰線・出来高急増・支持帯割れなどは、早期 EXIT を検討する材料**

「材料」「検討する」という言葉のままにしてある。

**これらを条件式に落として自動 EXIT にしない。**

### 6.3 RESEARCH_ONLY — 本番ルールにしないもの

以下はすべて研究コード（`src/swing_screener/research/`）にのみ存在する。

本番の判定には一切使われていない。

| 条件 | 研究での位置づけ | 採用しない理由 |
|---|---|---|
| 警戒陰線の機械判定（VARIANT A/B/C） | WARNING をいつ有効化するかの 3 案 | A は早すぎ（突破翌日に 64%）、B/C は WARNING なしが 27〜36% |
| `warning_low` 割れの扱い（LOW/CLOSE/STRUCTURAL） | 割ったあとどうするかの 3 案 | CLOSE_BREAK は日足運用と整合的だが、正式採用はしていない |
| `reference_high` の定義（RH-A〜RH-E 5案） | 「調整終了」の判定水準 | 32 件中 5 件しか結果が変わらず、利益保持も改善しない |
| 押し安値の機械確定 | trail stop の基準 | 直近の投げに張り付き、trail が立っても利益が残らない |
| trail stop の自動引き上げ | `押し安値 × 0.995` | 成立を増やすほど早降りが増える |
| 固定利確率 | — | **一度も導入していない。今後も探索しない** |

---

## 7. 保有中の表示 — CONFIRMED（表示ルールであって売買ルールではない）

本番ツールは保有銘柄について「売れ」と言わない。

代わりに **今日チャートを見るべき理由**を 3 段階で出す。

| 段階 | 意味 | 出る条件（表示のための整理） |
|---|---|---|
| **SCENARIO_RISK** | 買った理由が残っているか確認する | 終値が 初期STOP以下 / 買ったときのレンジ下限割れ / MA25割れ |
| **CAUTION** | 形に注意が要る | 大陰線 / 陰線＋出来高急増 / 直近の局所安値割れ |
| **REVIEW** | 一度チャートを見る | 直近陰線の安値割れ / 元レンジ上限に到達・突破済み |

- **どれも売買指示ではない。** 該当しても売らなくてよいし、該当しなくても売ってよい
- 使う閾値は既存の `experimental.yaml` の値だけ
  - 大陰線 3.0%
  - 出来高急増 1.8倍
- **EXIT 用の新しい閾値は 1 つも増やしていない**
- 実装は `src/swing_screener/review.py`

### シナリオ確認欄 — CONFIRMED（参考情報）

毎日「買った理由がまだ残っているか」を確認できるよう、次を表示する。

- 上昇トレンド維持
- MA25維持
- 元レンジ上限突破済み
- 重要支持帯を保っている
- 大きな陰線が出ていない
- 出来高が急増していない
- 直近の局所安値を保っている

**✗ が付いても自動の売却条件にはならない。**

---

## 8. トレード記録 — CONFIRMED

実際の売買を台帳（`data/trades.csv`）に記録する。

購入:

```text
code
entry_date
entry_price
quantity
original_range_lower
original_range_upper
initial_stop
entry_reason
memo
```

売却:

```text
exit_date
exit_price
exit_reason
exit_memo
```

`exit_reason` は自由記述。

記入例として、

```text
initial_stop
scenario_break
warning_candle
support_break
profit_protection
discretionary
other
```

を用意しているが、**これらは自動判定ルールではない**。

`original_range_*` と `initial_stop` は **ENTRY 時点の値を固定して持つ**。

毎日再計算するとレンジが動き、「何を根拠に買ったのか」が失われるため。

---

## 9. 一覧（確定 / 暫定 / 研究のみ）

### CONFIRMED

```text
価格帯                  2,000 〜 7,000円
移動平均                MA25（単純移動平均・終値）
上昇トレンドの必須条件   close > MA25
短期レンジ              3〜10営業日 / 境界は zone / 総合品質で成立を判定
下限反応                2回で満点の評価軸。必須条件ではない（§3.3）
ENTRYトリガー           終値 > 前日高値
初期STOP                range_lower × 0.995
STOPの性質              最大損失を保証しない（ギャップダウン）
ポジションサイズ         自動化しない
EXIT                    自動化しない（人間判断）
出来高                  補助評価のみ
新規の上限ブレイク探索   行わない
```

### PROVISIONAL

```text
near.max_position_in_range   0.65
← 上側ENTRY除外の安全ガード。最適値ではない

near.lower_threshold_pct     2.0
near.lookback_days           3
near.break_tolerance_pct     1.5
ma_slope.*                   MA25上向きの判定方法
swing.*                      fractal / pivot_window=2
range_zone.*                 0.7%
range_quality.*              品質の重みと除外条件
volume.*                     出来高の状態区分
```

### RESEARCH_ONLY

```text
警戒陰線の機械判定（VARIANT A / B / C）

warning_low 割れの扱い
（LOW_BREAK / CLOSE_BREAK / STRUCTURAL_BREAK）

reference_high の定義
（HOLDING_HIGH / WARNING_HIGH / PRE_WARNING_CLOSE_HIGH /
 WARNING_OPEN / PRE_WARNING_HIGH）

押し安値の機械確定

trail stop の自動引き上げ
（押し安値 × 0.995）

同日順序の仮定
（REHIGH優先 / EXIT優先）

固定利確率
（そもそも導入していない）
```

---

## 10. このルールを変更する手順

1. **フォワードで新しい事例を貯める**（`swing daily` を毎日実行する）
2. 10〜30 件程度の新規 ENTRY イベントが貯まってから
3. **困っている 1 点だけ**を対象に研究を再開する
4. 結論が出たらこの文書を改訂し、`config.yaml` / `experimental.yaml` を変える

**やらないこと**:

- 過去 32 件の成績を改善するための閾値探索
- 「仮想利益がいちばん高かった案」の自動採用
- 複数の条件を同時に動かすこと（何が効いたか分からなくなる）